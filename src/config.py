# 统一配置文件：所有超参数和路径集中在此，修改时无需改动其他模块

# ── 数据 ─────────────────────────────────────────────────────────────────────
DATA_PATH = './data/poems.txt'   # 语料库路径
SEQ_LEN   = 24                   # 训练样本的序列长度 L（滑动窗口大小）

# ── 模型结构 ──────────────────────────────────────────────────────────────────
EMBEDDING_SIZE = 256    # 词嵌入维度：离散字 ID → 连续稠密向量
HIDDEN_SIZE    = 512    # RNN 隐藏单元数：越大模型容量越强，但训练越慢
NUM_LAYERS     = 2      # RNN 堆叠层数：深层 RNN 能捕捉更复杂的长程依赖
DROPOUT        = 0.2    # Dropout 概率：训练时随机失活神经元，防止过拟合

# ── 训练 ─────────────────────────────────────────────────────────────────────
BATCH_SIZE     = 32     # 每个 mini-batch 的样本数
LEARNING_RATE  = 1e-3   # Adam 优化器初始学习率
EPOCH_NUM      = 20     # 训练总轮数
MAX_GRAD_NORM  = 5.0    # 梯度裁剪阈值：把所有参数梯度的 L2 范数压制在此值以内

# ── 生成（推理） ───────────────────────────────────────────────────────────────
DEFAULT_LINE_NUM   = 4    # 默认生成行数（每行含上下句各 LINE_LEN 字）
DEFAULT_LINE_LEN   = 7    # 默认每句字数（五言=5，七言=7）
DEFAULT_TEMPERATURE = 1.0 # 采样温度：>1 更随机多样，<1 更保守集中，=1 原始分布
